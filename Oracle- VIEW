VIEW이란?
NAMED SELECT문

--EMP테이블의 파일 위치 찾기

SELECT TABLE_NAME, B.TABLESPACE_NAME, C.FILE_NAME
FROM DBA_TABLES A, DBA_TABLESPACES B, DBA_DATA_FILES C
WHERE A.TABLESPACE_NAME = B.TABLESPACE_NAME
AND B.TABLESPACE_NAME = C.TABLESPACE_NAME
AND TABLE_NAME = 'EMP'
AND B.TABLESPACE_NAME = 'USERS';

--뷰 생성 및 수정

CREATE OR REPLACE VIEW 뷰이름
AS
테이블 형식

--뷰 생성(강제 OR 비강제)

존재 하지 않는 테이블을 가지고 뷰를 만들면 오류가 나지만 FORCE 옵션을 사용하면 일단은 뷰를 만들게 됨.

CREATE FORCE VIEW 뷰이름
AS
존재하지 않는 테이블이름.
