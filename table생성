CREATE TABLE 테이블이름(
    컬럼이름 컬럼타입);

ex)
create table tutorial_tb1(
    id INT NOT NULL AUTO_INCREMENT,
    title VARCHAR(100) NOT NULL,
    author VARCHAR(40) NOT NULL,
    date DATE,
    PRIMARY KEY(id)
    );
