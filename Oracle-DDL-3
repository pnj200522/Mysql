##
제약조건을 사용하지 않겠다.

ALTER TABLE T2_EMP
DISABLE CONSTRAINT EMP_FK;

다시 사용하겠다.

ALTER TABLE T2_EMP
ENABLE CONSTRAINT EMP_FK;

##
컬럼을 사용하지 않겠다.

ALTER TABLE T2_EMP
SET UNUSED COLUMN SAL;

사용하지 않는 컬럼 갯수를 보겠다.

SELECT * FROM USER_UNUSED_COL_TABS;

##
컬럼이름 변경

ALTER TABLE T2_EMP
RENAME COLUMN ENAME TO NAME;

##
READ ONLY

ALTER TABLE T2_EMP
READ ONLY;

READ ONLY 풀기

ALTER TABLE T2_EMP READ WRITE;
