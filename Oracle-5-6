## JOIN -USING

SELECT EMPNO, SAL, DEPTNO AS EDNO, DNAME
FROM EMP E JOIN DEPT D
USING(DEPTNO);

NON-JOIN PREDICATE추가 시

#WHERE#
SELECT EMPNO, SAL, DEPTNO AS EDNO, DNAME
FROM EMP E JOIN DEPT D
USING(DEPTNO)
WHERE SAL > 1500;  --오류 안남.

#AND#
SELECT EMPNO, SAL, DEPTNO AS EDNO, DNAME
FROM EMP E JOIN DEPT D
USING(DEPTNO)
AND SAL > 1500;  --오류남

#### USING 절에 사용된 컬럼은 어디에서나 테이블 알리아스로 한정하지 말것...
